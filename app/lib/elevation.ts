// DLS 2.0 Elevation Tokens — synced from memory/reference_dls_elevation.md

/** Diffuse ambient shadow — cards, floating elements */
export const ELEVATION_CARD = "0px 2px 32px 0px rgba(0,0,0,0.05)";

/** Shadow cast upward — fixed bottom elements (footer, button group, chat input) */
export const ELEVATION_ABOVE = "0px -6px 8px 0px rgba(0,0,0,0.05)";

/** Shadow cast downward — fixed top elements (app bar on scroll) */
export const ELEVATION_BELOW = "0px 6px 8px 0px rgba(0,0,0,0.05)";
